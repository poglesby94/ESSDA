
import { formatNews } from 'essda/helpers/format-news';
import { module, test } from 'qunit';

module('Unit | Helper | format news');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatNews([42]);
  assert.ok(result);
});

